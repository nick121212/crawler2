server {
    listen 10.25.254.241:8081;
    resolver 8.8.8.8;
    #resolver 223.5.5.5;
    resolver_timeout 5s;
    location / {
        proxy_pass $scheme://$http_host$request_uri;
        proxy_set_header Host $http_host;
        proxy_buffering off;
        #proxy_buffers 256 4k;
        #proxy_max_temp_file_size 0;
        #proxy_connect_timeout 5;
        #allow 127.0.0.1;
        #deny all;
    }
}